// Imported from node_modules/history folder using static routes
import createHistory from 'history/createBrowserHistory'

const history = createHistory()

// Export history object. To use history.push method, just import 'history' to any file you want
export default history
